name: linter-checker
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: poetry install
        run: pip install poetry
      - name: install dependencies
        run: make install
      - name: flake8
        run: make lint
